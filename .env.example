PORT=3000
JWT_SECRET=
MONGO_URI=
NODE_ENV=DEVELOPMENT

# Email (SMTP) — only required in production
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@helthstack.com
CLIENT_URL=http://localhost:5173